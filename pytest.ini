[pytest]
# Put the repo root on sys.path so tests can `from src... import ...`
# regardless of how pytest is invoked (plain `pytest`, `python -m pytest`,
# or via a command proxy). pytest >= 7.0 honours `pythonpath`.
pythonpath = .
testpaths = tests
